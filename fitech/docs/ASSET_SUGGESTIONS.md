# Asset search suggestions (Panel de Entrenamiento & app)

Use these search terms on **unDraw**, **Storyset**, **Blush**, or **IRA Design** to find replacement SVGs/illustrations that match each screen.

| Current asset | Use for | Search terms |
|---------------|--------|--------------|
| `diet_icon.png` | Dietas / planes de alimentación | `healthy eating`, `nutrition`, `meal plan`, `diet`, `food plan` |
| `workout_icon.png` | Rutinas / entrenamiento | `fitness`, `workout`, `gym`, `exercise`, `training`, `dumbbell` |
| `contract_icon.png` | Contratos / servicios / pagos / reseñas | `agreement`, `document`, `handshake`, `payment`, `wallet`, `rating`, `feedback` |
| `exercises_icon.png` | Registro de entrenamientos | `fitness tracker`, `checklist`, `calendar`, `exercise log`, `workout log` |

**Tips**
- **unDraw** (undraw.co): Pick one illustration style and use the same for all cards; set the accent color to your brand green.
- **Storyset** (storyset.com): Filter by “Sport” or “Fitness”; use static (non-animated) for card icons if you want consistency.
- Prefer **SVG** so you can tint the icon with `theme.primary` in code (e.g. via `react-native-svg`).
